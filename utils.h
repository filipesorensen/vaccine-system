#ifndef UTILS_H
#define UTILS_H

#include "project.h"

/* Function prototypes related to dates */
int daysInM(int month, int year);
int verifyDate(Date date);
int compareDates(Date d1, Date d2);

#endif /* UTILS_H */